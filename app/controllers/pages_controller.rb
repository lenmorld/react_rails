class PagesController < ApplicationController
    # NOTE that these are optional now, since they're empty anyways
    #       route still works without these, as long as the views/ page matches the route
    def home
    end

    def react
    end
end
